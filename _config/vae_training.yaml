# GenNN-InverseMicro: VAE Configuration File for training

model_params:
  input_channels: 3
  latent_dim: 64
  hidden_dims: [32, 64, 128, 256]
  input_size: 96

training_params:
  batch_size: 16
  epochs: 20
  learning_rate: 0.0001
  beta: 0.00005  # Beta-VAE weight
  device: "cuda" # or "cpu"

pth_path: "models/vae/best_vae.pth"